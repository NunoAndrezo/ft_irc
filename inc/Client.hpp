#ifndef CLIENT_HPP
# define CLIENT_HPP

#include <map>
#include <string>

class Client {
	private:
		std::map<std::string, bool> _channels //name client and whether is operator

	public:
		// constructors and destructors

		// getters/setters



};

#endif